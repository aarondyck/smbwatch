# /etc/systemd/system/smbwatch.timer

[Unit]
Description=Run smbwatch service every 5 minutes

[Timer]
# Use OnCalendar for a robust, cron-like schedule that runs every 5 minutes.
OnCalendar=*-*-* *:0/5:0
AccuracySec=1s
Persistent=true

[Install]
WantedBy=timers.target
